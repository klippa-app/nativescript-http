// iOS is direct copy of core, it already uses the internal http-client, so also our replacement.
export * from "@nativescript/core/ui/image-cache";
export {Cache as ImageCache} from "@nativescript/core/ui/image-cache";